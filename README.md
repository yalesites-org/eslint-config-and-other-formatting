# YaleSites [Prettier](https://prettier.io) config.

## Installation

```bash
$ npm install --save-dev @yalesites/config-prettier
```

Add the following to `package.json`:

```jsonc
{
  // ...
  "prettier": "@yalesites/config-prettier"
}
```
